# name: CI

# on:
#     push:
#         branches: [ master ]
#     pull_request:
#         branches: [ master ]
#     workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2
#     - name: Deploying virtimus website project
#       env:
#         HEROKU_API_KEY: ${{env.HEROKU_API_KEY}}
#         APP_NAME: virtimus
#       run: |
#         docker build -t vimmvc .
#         docker tag vimmvc registry.heroku.com
#         #sudo docker login --username=_ --password=5c0c5722-b31c-4077-899a-9821b56de06c registry.heroku.com
#         docker login --username=_ --password=$(heroku auth:token) registry.heroku.com
#         heroku container:push web -a $APP_NAME
#         heroku container:release web -a $APP_NAME



# name: virtimus-project
# on:
#   push:
#     branches:
#       - master
#   pull_request:
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: akhileshns/heroku-deploy@v3.12.14 # This is the action
#         with:
#           #heroku_api_key: e40220a2-b4d7-4645-9a68-cb7431d6d206
#           heroku_api_key: 5c0c5722-b31c-4077-899a-9821b56de06c
#           heroku_app_name: virtimus #Must be unique in Heroku
#           heroku_email: onimsi10@yahoo.com
#           justlogin: true
#           usedocker: true
#       - run: heroku auth:whoami


name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Login to Heroku Container registry
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:login 
    - name: Login in to container registry
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:push -a ${{ secrets.HEROKU_APP_NAME }} web 
    - name: Release
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:release -a ${{ secrets.HEROKU_APP_NAME }} web   
